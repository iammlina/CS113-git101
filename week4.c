#include <stdio.h>
#include <stdlib.h>//
char name [5]; //กำหนดขนาดเสมอ ใช้เเทนสตริง
int main()
{
    // printf("Enter name : ");
    // gets(name);
    // fgets(name, 20,stdin); // กำหนดขนาด บัฟเฟอร์ได้ เป็นที่เก็บข้อมูลชั่วคราว
    // printf("name = %s\n",name);
    fgets(n, 5,stdin);
    int i = atoi(n);// เปลี่ยน arry to integer
    printf("n = %s ,i = %d",name,i);
    
}
